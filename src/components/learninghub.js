import React from 'react';
import NewLearningHub from './NewLearningHub';

// This is just a wrapper component to redirect to our working version
const LearningHub = () => {
  return <NewLearningHub />;
};

export default LearningHub;
